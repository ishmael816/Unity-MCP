# Unity Dev Agent - 规格说明书 v2.0

> **文档编号**: UDA-SPEC-002  
> **版本**: v2.0  
> **状态**: 草案  
> **最后更新**: 2026-02-26  
> **架构方案**: 混合方案（MCP 工具 + Agent Prompt，AI 客户端驱动）

---

## 0. 实现复杂度评估

### 0.1 总体评估

| 维度 | 评级 | 说明 |
|------|------|------|
| **总体复杂度** | ⭐⭐⭐ 中等 | 混合方案大幅降低了独立 Agent 的开发成本 |
| **核心风险数** | 3 个 | Domain Reload、编译等待时序、LLM 上下文溢出 |
| **可行性** | ✅ 高 | 52 个 MCP 工具已覆盖 90% 所需能力 |

### 0.2 各模块复杂度明细

| 模块 | 复杂度 | 说明 |
|------|--------|------|
| Agent System Prompt 设计 | ⭐⭐ | ReAct 策略编码为 Prompt，需反复调优 |
| 编译检测与等待机制 | ⭐⭐⭐ | 需新增 MCP 工具：`compilation-wait`，处理异步编译 + Domain Reload |
| 截图验证（GameView + SceneView） | ⭐⭐ | 工具已存在，只需 Prompt 引导 LLM 分析截图 |
| 日志分析 | ⭐ | `console-get-logs` 已完备 |
| Agent 状态管理工具 | ⭐⭐⭐ | 需新增 MCP 工具：任务状态持久化与恢复 |
| 道路生成用例验证 | ⭐⭐ | 端到端测试，调优 Prompt |
| 集成测试 & 调优 | ⭐⭐ | 完整流程跑通 |

### 0.3 关键风险矩阵

| ID | 风险 | 概率 | 影响 | 缓解策略 |
|----|------|------|------|----------|
| R1 | **Domain Reload 断连** — `script-update-or-create` + `asset-refresh` 触发编译后，Unity 重载 AppDomain，SignalR 连接可能中断 | 高 | 高 | 新增 `compilation-wait` 工具，内部处理重连等待；Plugin 侧用 `[InitializeOnLoad]` 自动重连 |
| R2 | **编译时序不确定** — `asset-refresh` 返回后编译可能尚未完成，Agent 过早读取 log 会得到过期数据 | 高 | 中 | `compilation-wait` 工具内部轮询 `EditorApplication.isCompiling`，直到编译完成才返回 |
| R3 | **LLM 上下文溢出** — 多轮 ReAct 循环 + 截图 base64 + 代码 + 日志，容易超出 context window | 中 | 高 | Prompt 中设计"摘要压缩"策略：每 3 轮迭代将历史压缩为摘要；截图使用低分辨率 |
| R4 | **Vision API 判断不准** — LLM 对 Unity 3D 场景的视觉理解可能有偏差 | 中 | 中 | 截图验证作为"辅助参考"而非"硬性判断"，结合 Log 分析综合决策 |
| R5 | **无限修复循环** — 同一错误反复出现导致 Agent 卡死 | 低 | 高 | 硬性终止条件：最大 10 次迭代、连续 3 次同一错误则求助用户 |

---

## 1. 引言

### 1.1 目的

本文档定义 **Unity Dev Agent** 的功能规格、行为规格和验收标准。该 Agent 基于 **混合架构**（MCP 工具 + Agent System Prompt），利用现有 AI 客户端（如 Cursor / Claude Desktop）作为执行引擎，通过 ReAct 策略自主完成 Unity 开发任务。

### 1.2 架构定位

```
┌─────────────────────────────────────────────────────────────────┐
│                    AI 客户端 (Cursor / Claude Desktop)           │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │           Agent System Prompt (ReAct 策略)                │  │
│  │  "你是一个 Unity 开发 Agent，按以下流程工作..."           │  │
│  └───────────────────────────────────────────────────────────┘  │
│                              │                                   │
│                     调用 MCP 工具                                │
│                              ▼                                   │
├─────────────────────────────────────────────────────────────────┤
│              Unity-MCP-Server (.NET 9.0, SignalR Hub)            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │ 现有52个工具  │  │ 新增Agent工具 │  │ Agent Prompts        │  │
│  │ (script/     │  │ (compliation │  │ (react-dev-agent     │  │
│  │  screenshot/ │  │  -wait,      │  │  road-gen-agent)     │  │
│  │  console...) │  │  task-state) │  │                      │  │
│  └──────────────┘  └──────────────┘  └──────────────────────┘  │
│                              │                                   │
│                         SignalR                                  │
│                              ▼                                   │
├─────────────────────────────────────────────────────────────────┤
│              Unity-MCP-Plugin (Unity 2022.3+)                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Editor API 层：Script / Screenshot / Console / Editor   │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

**关键设计决策**：Agent 的"大脑"是 AI 客户端中的 LLM，Agent 的"策略"编码在 System Prompt 中，Agent 的"手脚"是 MCP 工具。我们不新建独立 Agent 进程。

### 1.3 适用范围

- Agent 的全生命周期：需求理解 → Spec 生成 → 代码生成 → 编译验证 → 运行时验证
- 首个验证用例：**道路生成工具（Road Generation Tool）**

### 1.4 参考文档

- MCP Protocol Specification
- Unity-MCP-Plugin API（52 Tools, 48 Prompts）
- ReAct: Synergizing Reasoning and Acting in Language Models (Yao et al., 2022)

---

## 2. 系统设计

### 2.1 新增 MCP 工具清单

以下工具需要新开发，注册到现有的 MCP Server / Plugin 中：

| 工具 ID | 位置 | 功能 | 优先级 |
|---------|------|------|--------|
| `compilation-wait` | Plugin | 触发 `AssetDatabase.Refresh()`，等待编译完成，返回编译结果（成功/失败+错误列表） | P0 |
| `compilation-status` | Plugin | 查询当前编译状态（idle/compiling/succeeded/failed）+ 错误详情 | P0 |
| `agent-task-save` | Plugin | 将当前任务状态（需求、Spec、迭代次数、代码版本）持久化到 `EditorPrefs` 或 JSON 文件 | P1 |
| `agent-task-load` | Plugin | 从持久化存储恢复任务状态 | P1 |
| `scene-save-backup` | Plugin | 在验证前自动备份当前场景 | P1 |
| `scene-restore-backup` | Plugin | 验证失败后恢复场景备份 | P1 |

### 2.2 新增 MCP Prompt 清单

| Prompt 名称 | 功能 |
|-------------|------|
| `react-dev-agent` | **核心 Agent System Prompt**，定义 ReAct 循环策略、工具调用规范、终止条件 |
| `road-generation-tool` | **道路生成用例 Prompt**，包含领域知识（Mesh 生成、Bézier 曲线、Editor 工具模式等） |

### 2.3 现有工具映射

Agent 在各阶段需要调用的**现有**工具：

| 阶段 | 工具 ID | 用途 |
|------|---------|------|
| 代码生成 | `script-update-or-create` | 写入 C# 脚本到 Unity 项目 |
| 代码阅读 | `script-read` | 读取已有脚本内容 |
| 编译触发 | `asset-refresh` | 触发 Unity 编译（已有，但将被 `compilation-wait` 替代/包装） |
| 日志检查 | `console-get-logs` | 获取 Error/Warning 日志 |
| 截图-GameView | `screenshot-game-view` | 捕获游戏视图 |
| 截图-SceneView | `screenshot-scene-view` | 捕获场景视图 |
| PlayMode 控制 | `editor-application-set-state` | 进入/退出 PlayMode |
| PlayMode 查询 | `editor-application-get-state` | 查询当前编辑器状态 |
| 场景操作 | `scene-save` / `scene-open` | 保存/打开场景 |
| GameObject 操作 | `gameobject-create` / `gameobject-find` | 创建/查找游戏对象 |
| 组件操作 | `gameobject-component-add` / `gameobject-component-modify` | 添加/修改组件 |
| 动态执行 | `script-execute` | Roslyn 动态编译执行 C# 代码（不触发 Domain Reload） |
| 测试运行 | `tests-run` | 运行 Unity Test Runner |
| 包管理 | `package-add` / `package-list` | 添加/查询 UPM 包 |

---

## 3. ReAct 循环引擎规格

### 3.1 总体流程

```
用户输入需求
     │
     ▼
┌──────────────────┐
│  Phase 1: SPEC   │  ◄── 与用户对话，收集需求，生成技术方案，用户确认
└────────┬─────────┘
         │ 用户确认 "开始开发"
         ▼
┌──────────────────┐
│  Phase 2: BUILD  │  ◄── ReAct 循环：生成代码 → 编译 → 修复 → 直到编译通过
│  (编译循环)       │
└────────┬─────────┘
         │ 编译通过
         ▼
┌──────────────────┐
│  Phase 3: VERIFY │  ◄── ReAct 循环：运行 → 截图 → 日志分析 → 修复 → 直到验证通过
│  (验证循环)       │
└────────┬─────────┘
         │ 验证通过
         ▼
┌──────────────────┐
│  Phase 4: DELIVER│  ◄── 向用户展示最终结果，提供使用说明
└──────────────────┘
```

### 3.2 Phase 1: SPEC（需求规格阶段）

**目标**：从模糊的用户需求中提炼出精确的技术方案。

**Agent 行为**：
1. **Observe**：解析用户输入的自然语言需求
2. **Think**：识别缺失信息（参数、约束、偏好）
3. **Act**：向用户提出澄清问题（最多 3 轮）
4. **Reflect**：信息是否足够生成 Spec？

**输出物 — 技术 Spec 文档**：
```markdown
## [工具名称] 技术规格

### 1. 功能需求
- 核心功能点列表
- 输入参数定义（名称、类型、范围、默认值）
- 输出/效果定义

### 2. 技术设计
- 脚本文件清单（路径、类名、职责）
- 核心算法描述（伪代码）
- Unity API 依赖项

### 3. UI 设计（如适用）
- EditorWindow 布局描述
- 控件清单与交互说明

### 4. 验证标准
- 编译验证：0 Error, 0 Warning（Obsolete 除外）
- 视觉验证：自然语言描述预期截图画面
- 日志验证：预期日志输出 / 不允许出现的异常
```

**用户确认**：Spec 生成后必须等待用户回复 "确认" / "开始开发" / 提出修改意见。最多 3 轮修订。

### 3.3 Phase 2: BUILD（编译循环）

**目标**：生成可编译通过的 C# 代码。

**单次迭代流程**：
```
┌─── Observe ───┐
│ 读取上一轮编译   │
│ 错误信息         │
│ (console-get-logs│
│  logType=Error)  │
└───────┬────────┘
        ▼
┌─── Think ─────┐
│ 分析错误原因     │
│ 决定修复策略：   │
│ - 增量修复       │
│ - 全量重写       │
│ - 添加 using    │
│ - 添加 Package  │
└───────┬────────┘
        ▼
┌─── Act ───────┐
│ 调用工具：       │
│ 1. script-update│
│    -or-create   │
│ 2. compilation  │
│    -wait        │
└───────┬────────┘
        ▼
┌─── Reflect ───┐
│ 检查编译结果：   │
│ ✅ 通过 → Phase 3│
│ ❌ 失败 → 下一轮│
│ 🔄 超限 → 求助  │
└────────────────┘
```

**约束条件**：
- 最大迭代次数：**10 次**
- 连续相同错误阈值：**3 次**（超过则暂停，请求用户介入）
- 编译等待超时：**60 秒**
- 代码存放路径：`Assets/AutoGenerated/{ToolName}/`

**首次生成策略**：
- 基于 Spec 一次性生成所有文件
- 命名空间：`AutoGenerated.Tools.{ToolName}`
- 必须包含 `[MenuItem]` 入口（Editor 工具类）
- 必须包含关键日志输出（`Debug.Log("[ToolName] xxx")`）

**修复策略选择**（由 Agent 自动判断）：
| 错误类型 | 策略 |
|----------|------|
| 语法错误（1-3处） | 增量修复 — 仅修改出错行 |
| 类型/命名空间缺失 | 添加 `using` 或调用 `package-add` |
| 逻辑结构错误（>5处） | 全量重写当前文件 |
| 跨文件依赖错误 | 读取相关文件 (`script-read`) 后统一修复 |

### 3.4 Phase 3: VERIFY（验证循环）

**目标**：确认生成的工具在 Unity 中可正常运行，视觉效果和行为符合预期。

**单次迭代流程**：
```
┌─── Observe ────────────┐
│ 1. scene-save-backup    │  ← 备份当前场景
│ 2. script-execute       │  ← 通过 Roslyn 动态执行工具入口
│    或 editor-set-state  │  ← 进入 PlayMode
│ 3. screenshot-game-view │  ← 捕获 GameView 截图
│ 4. screenshot-scene-view│  ← 捕获 SceneView 截图
│ 5. console-get-logs     │  ← 获取运行日志
│ 6. editor-set-state     │  ← 退出 PlayMode
└──────────┬─────────────┘
           ▼
┌─── Think ──────────────┐
│ 多维度综合分析：          │
│                          │
│ 📸 视觉分析（Vision）    │
│ "截图中是否显示了道路     │
│  Mesh？纹理是否正确？    │
│  UI 窗口是否存在？"      │
│                          │
│ 📋 日志分析              │
│ "是否有 Error？          │
│  是否有预期的成功日志？   │
│  Warning 数量是否合理？" │
│                          │
│ 📊 综合判定              │
│ 视觉 OK + 日志 OK → 通过│
│ 任一不 OK → 定位问题     │
└──────────┬─────────────┘
           ▼
┌─── Act ────────────────┐
│ 根据问题类型：            │
│ - 视觉异常 → 修改代码逻辑│
│ - 运行时错误 → 修复异常   │
│ - 功能缺失 → 补充实现     │
│ → 返回 Phase 2 重新编译   │
└──────────┬─────────────┘
           ▼
┌─── Reflect ────────────┐
│ ✅ 全部通过 → Phase 4   │
│ ❌ 失败 → 下一轮         │
│ 🔄 超限 → 向用户展示当前│
│    状态并请求指导          │
└────────────────────────┘
```

**视觉验证规格**：
```yaml
visual_validation:
  sources:
    - screenshot-game-view  (1280x720)
    - screenshot-scene-view (1280x720)
  
  analysis_prompt: |
    请分析以下 Unity 截图，对照技术 Spec 中的预期效果：
    1. 预期的 3D 对象是否存在且可见？
    2. 材质/颜色是否正确？
    3. UI 窗口（如有）是否正常渲染？
    4. 是否存在明显的视觉缺陷（穿模、黑屏、闪烁）？
    
    返回格式：
    - visual_pass: true/false
    - issues: [问题描述列表]
    - suggestions: [修复建议列表]
```

**日志验证规格**：
```yaml
log_validation:
  error:
    max_count: 0
    critical_patterns:
      - "NullReferenceException"
      - "IndexOutOfRangeException"
      - "MissingReferenceException"
      - "StackOverflowException"
  
  warning:
    max_count: 5
    ignore_patterns:
      - "Obsolete"
      - "deprecated"
  
  expected_logs:  # 由 Spec 阶段定义
    - pattern: "[{ToolName}] Initialized"
      required: true
    - pattern: "[{ToolName}] Generation complete"
      required: true
```

**安全机制**：
- PlayMode 最长运行 **30 秒**，超时自动退出
- 验证前自动备份场景，失败后可恢复
- Error 日志超过 **5 条**立即终止验证

### 3.5 Phase 4: DELIVER（交付阶段）

**Agent 行为**：
1. 向用户展示最终成果清单（文件列表、功能说明）
2. 提供使用指南（如何打开 EditorWindow、参数说明）
3. 展示最终截图作为效果证明
4. 输出**任务完成报告**（格式见 §3.6）
5. 询问用户是否满意或需要调整

### 3.6 任务完成报告规范（Task Completion Report）

**触发时机**：每当完成一个 **Phase** 或一项独立的 **子任务**（如"创建了一个新的 MCP 工具文件"、"修复了一个编译错误"等），Agent **必须**输出一份任务完成报告。

**报告分为两部分**：

#### 3.6.1 Part A — 结构化完成报告

使用以下 Markdown 模板输出：

```markdown
---
## 📋 任务完成报告

### 1. 任务摘要
- **任务名称**: [简洁描述]
- **所属阶段**: Phase X: [SPEC/BUILD/VERIFY/DELIVER]
- **状态**: ✅ 完成 / ⚠️ 部分完成 / ❌ 失败

### 2. 完成内容
| # | 操作 | 目标文件/对象 | 说明 |
|---|------|---------------|------|
| 1 | 创建/修改/删除 | `路径/文件名` | 具体说明 |
| 2 | ... | ... | ... |

### 3. 变更清单
- **新增文件**: [列表]
- **修改文件**: [列表]
- **删除文件**: [列表]
- **新增依赖**: [列表，如 UPM 包]

### 4. 验证指南（用户手动验证步骤）
> 以下步骤供用户手动验证 Agent 的工作成果：

1. [ ] **步骤 1**: [具体操作，例如"在 Unity 中打开项目，确认无编译错误"]
2. [ ] **步骤 2**: [具体操作]
3. [ ] **步骤 3**: [具体操作]

### 5. 已知问题 & 遗留项
- [如有，列出；如无，写"无"]

### 6. 下一步
- [描述接下来要做的事情]
---
```

#### 3.6.2 Part B — C# 单元测试

Agent 在完成 **Phase 2: BUILD** 后，**必须**编写并运行 C# 单元测试：

**测试文件规范**：
- **存放路径**: `Assets/AutoGenerated/{ToolName}/Tests/`
- **命名**: `{ClassName}Tests.cs`
- **测试框架**: Unity Test Framework (`NUnit`)
- **Assembly Definition**: 需创建 `Tests.asmdef`（引用 `UnityEngine.TestRunner` 和 `UnityEditor.TestRunner`）

**最低覆盖要求**：

| 测试类别 | 要求 | 示例 |
|----------|------|------|
| **冒烟测试** | 必须 | 类能实例化、核心方法不抛异常 |
| **输入边界** | 必须 | 参数为 0 / 负数 / 极大值时不崩溃 |
| **核心逻辑** | 必须 | 算法输出正确（如：Mesh 顶点数 = 预期值） |
| **Editor 集成** | 可选 | EditorWindow 能正常打开/关闭 |

**测试执行与报告**：
- Agent 使用 `tests-run` MCP 工具执行测试
- 将测试结果嵌入到任务完成报告中：

```markdown
### 7. 单元测试结果
| 测试名称 | 状态 | 耗时 | 备注 |
|----------|------|------|------|
| `RoadGeneratorTests.SmokeTest_CreateInstance` | ✅ Pass | 12ms | |
| `RoadGeneratorTests.BezierCurve_ZeroPoints_NoException` | ✅ Pass | 3ms | |
| `RoadGeneratorTests.MeshGeneration_VertexCount_Correct` | ✅ Pass | 45ms | |
| `RoadGeneratorTests.MeshGeneration_NegativeWidth_Clamped` | ✅ Pass | 5ms | |

**总计**: X 通过 / Y 失败 / Z 跳过
```

**失败处理**：
- 如有测试失败，Agent 进入修复循环（等同于 BUILD 阶段的编译修复循环）
- 测试全部通过后才能进入 Phase 3: VERIFY

#### 3.6.3 报告输出规则

| 场景 | 报告要求 |
|------|----------|
| 完成 Phase 1: SPEC | Part A（无 Part B） |
| 完成 Phase 2: BUILD | Part A + Part B（含单元测试） |
| 完成 Phase 3: VERIFY | Part A（可选补充集成测试） |
| 完成 Phase 4: DELIVER | Part A（最终汇总报告） |
| 完成独立子任务（如新建 MCP 工具） | Part A（精简版，可省略"验证指南"节） |

---

## 4. Agent System Prompt 规格

### 4.1 核心 Prompt 结构

```markdown
# Unity Dev Agent - System Prompt

## 身份
你是一个专业的 Unity 开发 Agent。你通过 MCP 工具直接操作 Unity Editor，
按照 ReAct（Reasoning + Acting）策略自主完成开发任务。

## 工作流程
你必须严格按照以下四个阶段工作：

### Phase 1: SPEC（需求规格）
- 分析用户需求，识别缺失信息
- 主动提出澄清问题（最多3轮）
- 生成技术 Spec 文档
- 等待用户确认后才能进入下一阶段

### Phase 2: BUILD（编译循环）
- 基于 Spec 生成 C# 代码
- 调用 `script-update-or-create` 写入文件
- 调用 `compilation-wait` 触发编译并等待结果
- 如编译失败，分析错误并修复（最多10轮）
- 每轮必须输出思考过程：
  ```
  🔍 观察：[编译错误内容]
  🧠 思考：[错误原因分析]
  🎯 行动：[修复方案]
  ```

### Phase 3: VERIFY（验证循环）
- 备份场景
- 执行工具功能（优先用 `script-execute`，必要时用 PlayMode）
- 截取 GameView + SceneView 截图
- 获取 Console 日志
- 综合分析视觉效果和日志
- 如不符合预期，修复并返回 Phase 2

### Phase 4: DELIVER（交付）
- 展示成果、使用说明、最终截图
- 询问用户是否满意

## 工具使用规范
- 写代码后必须触发编译：`script-update-or-create` → `compilation-wait`
- 检查编译结果：`compilation-status` 或 `console-get-logs(logType=Error)`
- 截图使用 1280x720 分辨率以节省 token
- 每次修复前先读取当前代码：`script-read`

## 终止条件
- ✅ 成功：编译通过 + 视觉验证通过 + 日志无 Error
- ❌ 失败：达到最大迭代次数（10次）
- ⚠️ 求助：连续3次相同错误 → 暂停并请求用户指导

## 输出格式
每个 ReAct 步骤必须使用以下格式：
🔍 **观察**：[当前状态描述]
🧠 **思考**：[分析与推理]
🎯 **行动**：[将要执行的工具调用]
🔄 **反思**：[行动结果评估，下一步计划]
```

### 4.2 道路生成用例 Prompt 补充

```markdown
# 道路生成工具 - 领域知识补充

## Unity 道路生成常用技术
- **Mesh 生成**：通过代码构建 vertices、triangles、uvs
- **Bézier 曲线**：用控制点定义道路路径，支持平滑弯道
- **EditorWindow**：自定义 Inspector 面板，提供参数调节 UI
- **Handles API**：在 SceneView 中绘制可交互的控制点手柄
- **ProceduralMesh**：运行时或 Editor 时生成 Mesh 并赋予 MeshFilter

## 推荐实现架构
1. `RoadGeneratorWindow.cs` — EditorWindow，提供 UI 面板
2. `RoadGenerator.cs` — 核心逻辑，Mesh 生成算法
3. `RoadData.cs` — ScriptableObject，保存道路配置数据
4. `RoadHandleEditor.cs` — 自定义 Editor，SceneView 控制点交互

## 验证标准
- 视觉：SceneView 中应可见一条道路 Mesh，有正确的 UV 和材质
- 日志：应输出 "[RoadGenerator] Road mesh generated with X vertices"
- UI：EditorWindow 应包含宽度、段数、曲线控制点等参数
```

---

## 5. 新增 MCP 工具详细规格

### 5.1 `compilation-wait`

```yaml
tool_id: "compilation-wait"
title: "Compilation / Wait"
description: >
  触发 AssetDatabase.Refresh() 并等待 Unity 编译完成。
  内部轮询 EditorApplication.isCompiling，直到编译结束。
  返回编译结果（成功/失败）和错误详情。

parameters:
  - name: timeout_seconds
    type: int
    default: 60
    description: "等待编译完成的最大秒数"

returns:
  type: object
  fields:
    - success: bool          # 编译是否通过
    - duration_ms: int       # 编译耗时
    - errors: string[]       # 编译错误列表
    - warnings: string[]     # 编译警告列表
    - script_compilation_failed: bool  # EditorUtility.scriptCompilationFailed

implementation_notes: |
  1. 调用 AssetDatabase.Refresh()
  2. 等待 EditorApplication.isCompiling 变为 true（或已经为 true）
  3. 轮询等待 EditorApplication.isCompiling 变为 false
  4. 检查 EditorUtility.scriptCompilationFailed
  5. 如 failed，收集 CompilerMessage[] 详情
  6. 注意：Domain Reload 期间 SignalR 可能断连，
     需在 Plugin 侧用 EditorPrefs 缓存结果，
     重连后立即返回缓存的编译结果
```

### 5.2 `compilation-status`

```yaml
tool_id: "compilation-status"
title: "Compilation / Status"
description: >
  查询当前 Unity 编译状态，不触发编译。

returns:
  type: object
  fields:
    - is_compiling: bool
    - script_compilation_failed: bool
    - error_count: int
    - errors: string[]      # 最近的编译错误
```

### 5.3 `agent-task-save` / `agent-task-load`

```yaml
tool_id: "agent-task-save"
title: "Agent / Task / Save"
description: >
  将当前 Agent 任务状态持久化，用于 Domain Reload 或连接断开后恢复。

parameters:
  - name: task_id
    type: string
    description: "任务唯一标识"
  - name: state_json
    type: string
    description: "JSON 格式的任务状态数据"

---

tool_id: "agent-task-load"
title: "Agent / Task / Load"
description: >
  从持久化存储恢复 Agent 任务状态。

parameters:
  - name: task_id
    type: string
    description: "任务唯一标识"

returns:
  type: string  # JSON 格式的任务状态
```

---

## 6. 非功能规格

### 6.1 性能指标

| 指标 | 要求 | 说明 |
|------|------|------|
| 单次代码生成 | < 30 秒 | LLM 响应时间 |
| 编译等待超时 | 60 秒 | `compilation-wait` |
| PlayMode 超时 | 30 秒 | 验证执行时间上限 |
| 截图分辨率 | 1280×720 | 平衡清晰度与 token 消耗 |
| 单次 ReAct 迭代 | < 2 分钟 | 含 LLM 思考 + 工具调用 |
| 最大迭代次数 | 10 次 | 防无限循环 |
| 端到端（简单工具） | < 30 分钟 | 从需求到交付 |

### 6.2 可靠性指标

| 指标 | 要求 |
|------|------|
| Domain Reload 恢复 | 100%（通过 `agent-task-save/load`） |
| SignalR 自动重连 | ≤ 5 秒内恢复 |
| 编译结果准确性 | 100%（不会误报成功） |
| 场景数据安全 | 验证前必须备份 |

---

## 7. 验收标准

### 7.1 功能验收（以道路生成工具为用例）

| ID | 验收项 | 标准 | 优先级 |
|----|--------|------|--------|
| AC-01 | **需求对话** | Agent 能主动询问道路类型（直线/曲线）、参数（宽度/段数）等 | P0 |
| AC-02 | **Spec 生成** | 生成包含文件清单、算法描述、验证标准的技术 Spec | P0 |
| AC-03 | **代码生成** | 生成 ≥1 个 C# 文件到 `Assets/AutoGenerated/RoadGenerator/` | P0 |
| AC-04 | **编译通过** | `compilation-wait` 返回 success=true，0 Error | P0 |
| AC-05 | **自动修复** | 编译失败时能分析错误并修复，≤5 轮内通过 | P0 |
| AC-06 | **视觉验证-GameView** | 截图中可见道路 Mesh 对象 | P1 |
| AC-07 | **视觉验证-SceneView** | 截图中可见道路 Mesh 及控制点（如有） | P1 |
| AC-08 | **日志验证** | Console 无 Error，包含预期的初始化日志 | P0 |
| AC-09 | **端到端闭环** | 从 "开发道路生成工具" 到 "工具可用" 完整流程 | P0 |
| AC-10 | **交付说明** | Agent 提供使用指南（MenuItem 路径、参数说明） | P1 |

### 7.2 技术验收

| ID | 验收项 | 标准 |
|----|--------|------|
| TC-01 | `compilation-wait` 工具 | 正确等待编译完成，Domain Reload 后能恢复 |
| TC-02 | `compilation-status` 工具 | 准确返回编译状态 |
| TC-03 | Agent Prompt | 在 Cursor 中加载后能按 ReAct 流程工作 |
| TC-04 | 截图分析 | LLM 能基于截图给出有意义的视觉反馈 |

---

## 8. 开发路线图

### Phase A — 基础设施
- [x] ~~实现 `compilation-wait` 工具~~ → 已有 `script-update-or-create` 的 Processing 模式自动处理编译等待，无需独立工具
- [x] 实现 `compilation-status` 工具 → `Compilation.Status.cs`
- [x] 实现 `agent-task-save` / `agent-task-load` 工具 → `AgentTask.Save.cs` / `AgentTask.Load.cs`

### Phase B — Agent Prompt
- [x] 编写 `react-dev-agent` 核心 System Prompt → `DevAgent.cs`
- [x] 编写 `road-generation-tool` 领域 Prompt → `DevAgent.RoadGeneration.cs`
- [ ] 在 CodeMaker 中端到端测试 Prompt 驱动的 ReAct 流程

### Phase C — 验证模块调优
- [ ] 调优截图分析 Prompt（GameView + SceneView）
- [ ] 实现 `scene-save-backup` / `scene-restore-backup`
- [ ] 完善错误修复策略（增量 vs 全量的自动判断）

### Phase D — 集成测试 & 打磨
- [ ] 道路生成工具端到端测试（≥5 次完整流程）
- [ ] 边界情况测试（编译超时、无限循环、上下文溢出）
- [ ] 文档完善、使用指南

---

## 9. 待确认事项

### 9.1 验证标准定义方式

**已决策**: 自然语言描述（由 Spec 阶段定义，LLM Vision 分析判断）

### 9.2 用户干预时机

**已决策 — 选项C**: 仅在 Spec 确认阶段和异常/无法自动解决时请求用户介入。正常 ReAct 循环中自动运行。连续 3 次相同错误或达到最大迭代次数时暂停求助。

### 9.3 代码质量要求

**已决策 — 选项B → 升级为选项C**: 符合 C# 基本规范（合理命名、XML 注释、适当的 namespace）。**必须包含 NUnit 单元测试**（冒烟测试 + 边界测试 + 核心逻辑测试），使用 `tests-run` 工具执行并在任务完成报告中输出结果（详见 §3.6）。

### 9.4 AI 客户端选择

**已决策**: CodeMaker（优先），Cursor 作为备选。当前设计兼容所有支持 MCP 的 AI 客户端。

---

## 10. 术语表

| 术语 | 定义 |
|------|------|
| ReAct | Reasoning + Acting，LLM 在推理和行动间交替的 Agent 模式 |
| MCP | Model Context Protocol，AI 与 Unity 通信的标准协议 |
| Domain Reload | Unity 编译 C# 后重载 AppDomain，导致所有内存对象重置 |
| SignalR | ASP.NET Core 的实时通信框架，Server 与 Plugin 之间的桥梁 |
| Roslyn | .NET 编译器平台，`script-execute` 使用它动态编译执行代码 |
| System Prompt | 注入给 LLM 的系统级指令，定义 Agent 的行为策略 |

---

**文档审批**:

| 角色 | 姓名 | 日期 | 意见 |
|------|------|------|------|
| 作者 | AI Agent | 2026-02-26 | 初稿 |
| 审核 | | | |
| 批准 | | | |

---

以上就是完整的 v2.0 Spec。核心变化总结：

1. **架构从"独立 Agent 进程"改为"混合方案"**，利用现有 AI 客户端驱动，大幅降低开发复杂度
2. **精确映射了 14 个现有 MCP 工具**到各流程阶段，只需新增 **6 个**工具
3. **ReAct 四阶段流程完整定义**：SPEC → BUILD → VERIFY → DELIVER
4. **重点解决了 3 个核心风险**：Domain Reload（`compilation-wait` 内部处理）、编译时序（轮询等待）、上下文溢出（摘要压缩策略）
5. **以"道路生成工具"作为首个端到端验证用例**，10 个验收标准